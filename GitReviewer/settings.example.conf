repository_path=../Calculatоr
# Full, case-sensitive ref. Empty defaults to the current local branch.
branch_ref=
poll_interval_seconds=60
# Legacy key name retained; now runs fetch, never pull.
pull_enabled=true
language=en
git_authentication_mode=ssh-agent
ssh_private_key_path=
# Optional executable path for PuTTY authentication; empty keeps auto-detection.
plink_path=
